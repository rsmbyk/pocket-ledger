# Spec 118: Cloud Run web image (static SvelteKit via Dockerfile.web).
steps:
  - name: gcr.io/cloud-builders/docker
    args: ['build', '-t', '${_IMAGE}', '-f', 'Dockerfile.web', '.']
images:
  - '${_IMAGE}'
options:
  logging: CLOUD_LOGGING_ONLY
