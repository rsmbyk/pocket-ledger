# Tasks 141: Transactions header date range (month or custom)

Accepted.

- [ ] Branch: `feat/141-transactions-date-range` after Accept
- [ ] **Red Vitest** default current month → today; past month last day; start≤end snap; Month↔Custom mapping
- [ ] **Green** helpers; session `mode` + `monthKey` / dates
- [ ] Header cluster: Month | Custom; live filter; title stays left
- [ ] Remove From/To from filter panel; Clear does not reset range; no date badge
- [ ] Playwright: default range; mode switch; Filters have no From/To
- [ ] Manual: Chrome month picker + custom DateFields (needs 135)
