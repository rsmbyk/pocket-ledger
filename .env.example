# Copy to .env.local (gitignored) for Wrangler CLI deploys.
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=

# Optional: report deploys to GitHub Environments (also set as a Cloudflare Builds secret).
# Fine-grained PAT: Deployments read/write on rsmbyk/pocket-ledger
GITHUB_DEPLOYMENTS_TOKEN=
